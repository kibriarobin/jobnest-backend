enum Role {
  CANDIDATE
  EMPLOYER
  ADMIN
}

enum JobType {
  REMOTE
  ONSITE
  HYBRID
}

enum ApplicationStatus {
  APPLIED
  SHORTLISTED
  INTERVIEW
  HIRED
  REJECTED
}

enum JobStatus {
  PENDING
  APPROVED
  REJECTED
  CLOSED
}